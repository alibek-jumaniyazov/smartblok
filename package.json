{
  "name": "smartblok",
  "version": "1.0.0",
  "private": true,
  "description": "SmartBlok — CRM/ERP for gas-block (aerated concrete) wholesale distribution",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "predev": "node scripts/ensure-env-db.mjs",
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "start": "npm run start:prod -w apps/api",
    "db:setup": "npm run db:setup -w apps/api",
    "db:reset": "npm run db:reset -w apps/api",
    "seed": "npm run seed -w apps/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
